# Copy to .env and fill in. Never commit .env.
# Supabase (staging project). Values come from the Supabase dashboard: Project Settings > API.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Radio test stream for the week-1 spike (real stream credentials come from Mario in week 6).
EXPO_PUBLIC_RADIO_STREAM_URL=
